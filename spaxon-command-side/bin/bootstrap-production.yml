server:
  port: 9000
spring:
  application:
    name: spaxon-command-side
  cloud:
    config:
      uri: http://spaxon-config:8888
      failFast: true
      retry:
        initialInterval: 2000
        maxAttempts: 30